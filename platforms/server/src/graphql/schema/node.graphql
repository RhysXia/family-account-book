union Node =
    SimpleUser
  | AccountBook
  | SavingAccount
  | Tag
  | FlowRecord
  | SavingAccountTransferRecord
  | SavingAccountHistory

type Query {
  node(id: ID!): Node
  nodes(ids: [ID!]!): [Node!]!
}
