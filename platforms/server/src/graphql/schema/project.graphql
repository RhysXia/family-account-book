type Project {
  id: Int!
  name: String!
  desc: String!
  admins: [User!]!
  members: [User!]!
}

input ProjectInput {
  name: String!
  desc: String!
  adminIds: [Int!]!
  memberIds: [Int!]!
}

type Mutation {
  createProject(user: ProjectInput!): Project!
}
