scalar DateTime

enum Direction {
  DESC
  ASC
}

input OrderBy {
  field: String!
  direction: Direction!
}

input Pagination {
  skip: Int
  take: Int
  orderBy: [OrderBy!]
}

interface Timestamp {
  createdAt: DateTime!
  updatedAt: DateTime!
}
