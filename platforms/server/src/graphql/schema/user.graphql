type User implements Timestamp {
  id: Int!
  username: String!
  email: String
  avatar: String
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Query {
  """
  获取当前登录用户信息
  """
  getCurrentUser: User!
  """
  根据模糊搜索用户列表
  """
  findUserListByUsernameLike(username: String!, limit: Int = 10): [User!]!
}

input SignUpUserInput {
  username: String!
  password: String!
  email: String
  avatar: String
}

input SignInUserInput {
  username: String!
  password: String!
  rememberMe: Boolean
}

type Mutation {
  signIn(user: SignInUserInput!): User!
  signUp(user: SignUpUserInput!): User!
}
